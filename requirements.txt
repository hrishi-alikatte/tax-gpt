# VaudTaxAI production dependencies — pinned for reproducibility.
# Last updated: 2026-05-13
# Update with: pip install --upgrade <pkg>, then pin new version here.

# Core
pydantic==2.11.4
python-dotenv==1.1.0

# UI (legacy desktop path; not shipped in container)
flet==0.21.2

# RAG / vectors
chromadb==1.0.7
pypdf==5.4.0

# Extraction
pdfplumber==0.11.6

# LangChain (legacy brain/rag.py path; migrate away in future)
langchain==0.2.17
langchain-core==0.2.43
langchain-community==0.2.19
langchain-openai==0.1.25
langchain-text-splitters==0.2.4
langgraph==0.2.74

# Legacy Groq adapter — DROP in Phase 3 cleanup
langchain-groq==0.1.9

# Azure OpenAI SDK
openai==1.76.2

# Groq SDK (demo fallback only)
groq==0.22.0

# Test / dev
pytest==8.4.0

# API
uvicorn==0.34.2
fastapi==0.115.9
python-multipart==0.0.20
